const Error_Types = Object.freeze({
    LOGGED_OUT: 'logged-out',
    PERMISSION_REJECTED: 'permission-rejected',
    INVALID_OPERATION: 'invalid-operation',
    JOB_ALREADY_ACCEPTED: 'job-already-accepted'
});

export { Error_Types };